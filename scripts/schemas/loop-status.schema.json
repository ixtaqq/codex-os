{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "LoopStatus",
  "type": "object",
  "additionalProperties": false,
  "required": ["status", "summary", "next_action"],
  "properties": {
    "status": {
      "type": "string",
      "enum": ["continue", "done", "blocked"],
      "description": "done = the exit condition verifiably holds. blocked = cannot proceed without a decision, credential, or fix outside this loop. continue = more passes needed."
    },
    "summary": {
      "type": "string",
      "description": "What this pass actually did and what the check reported."
    },
    "next_action": {
      "type": "string",
      "description": "The single next increment of work, or why the loop is stopping."
    }
  }
}
