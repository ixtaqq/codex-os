# Per-project Codex settings. Only include lines that differ from your global config.toml —
# an empty file is better than one restating defaults. Delete what you do not need.

# model = "gpt-5.6-sol"
# model_reasoning_effort = "medium"

# Sandbox for this repo: read-only | workspace-write | danger-full-access
# sandbox_mode = "workspace-write"

# Extra directories this project needs to write to, beyond the repo root.
# [sandbox_workspace_write]
# writable_roots = []

# Project-scoped MCP servers.
# [mcp_servers.example]
# command = "npx"
# args = ["-y", "@example/mcp-server"]
