# <Project name>

<One sentence: what this is and who uses it.>

Personal defaults live in `~/.codex/AGENTS.md`. This file covers only what is different here.
Delete any section below you cannot fill with something specific.

## Stack

- Language / runtime: <version>
- Framework: <name + version>
- Package manager: <npm | pnpm | uv | cargo | ...>

## Commands

Every command here must have been run and observed to work.

| Task | Command |
| --- | --- |
| install | `<cmd>` |
| dev | `<cmd>` |
| build | `<cmd>` |
| test | `<cmd>` |
| single test | `<cmd>` |
| lint | `<cmd>` |

## Layout

- `<dir>/` — <what lives here>
- `<dir>/` — <what lives here>

## Conventions

- <A rule the existing code actually follows — naming, error handling, module boundaries.>
- <Something a newcomer would get wrong.>

## Verification

Before calling a change done here: <the specific bar — which command, which manual check>.

## Gotchas

- <Non-obvious thing that will waste an hour otherwise.>
